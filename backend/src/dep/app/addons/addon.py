# from . import db,users
# import asyncio

# async def _LocalIDGet(localID):
#     user = await users.find_one({"localID":localID})
#     return user

# def WithLocalIDReturnUser(localID):
#     return asyncio.run(_LocalIDGet(localID))